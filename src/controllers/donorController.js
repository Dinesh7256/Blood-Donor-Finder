// Placeholder controller for donor search routes.
// This will handle finding donors near a location in a later phase.

const donorController = {
  searchDonors: async (req, res) => {
    res.status(200).json({ message: "Donor search endpoint pending implementation" });
  },
};

module.exports = donorController;
